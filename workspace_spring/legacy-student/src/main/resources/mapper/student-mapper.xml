<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.legacy_student.mapper.StuMapper">

    <resultMap id="cls" type="com.green.legacy_student.dto.ClassDTO">
        <id     column="CLASS_NUM"  property="classNum" />
        <result column="CLASS_NAME" property="className" />
    </resultMap>

    <resultMap id="stu" type="com.green.legacy_student.dto.StuDTO">
        <id     column="STU_NUM"    property="stuNum" />
        <result column="STU_NAME"   property="stuName" />
        <result column="STU_AGE"    property="stuAge" />
        <result column="CLASS_NUM"  property="classNum" />
    </resultMap>

    <!-- 학급 목록 조회 -->
    <select id="getClassList" resultMap="cls">
        SELECT CLASS_NUM
            , CLASS_NAME
        FROM CLASS_INFO2
    </select>

    <!-- 학생 목록 조회 -->
    <select id="getStuList" resultMap="stu">
        SELECT STU_NUM
            , STU_NAME
            , STU_AGE
        FROM STUDENT2
    </select>

    <!-- 선택한 반의 학생 목록 조회 -->
    <select id="getStuListAgain" resultMap="stu">
        SELECT STU_NUM
            , STU_NAME
            , STU_AGE
        FROM STUDENT2
        <if test="classNum != 0">
        WHERE CLASS_NUM = #{classNum}
        </if>
    </select>

    <!-- 선택한 반의 학생 목록 조회 -->
    <select id="getStuDetail" resultMap="stu">
        SELECT STU_NUM
            , STU_NAME
            , STU_AGE
        FROM STUDENT2
        WHERE STU_NUM = #{StuNum}
    </select>

</mapper>